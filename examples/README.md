# Examples

These examples show how to use ReminderBot in a few common scenarios.

- `basic.py` - the simplest way to run a job
- `config.example.json` - a starting configuration

Copy an example, tweak the values, and run it.